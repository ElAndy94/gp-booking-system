import React from "react";

// import classes from './GymTimetable.css';

const article = (props) => (
  <article>
        <h2>{props.title}</h2>
        <h1>{props.content}</h1>
    </article>
);
export default article;